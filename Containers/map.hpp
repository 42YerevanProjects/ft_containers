#pragma once

#include "../Utility/rb_tree_node.hpp"

namespace ft
{
    template < class Key,
               class T
               //class Compare = less<Key>,
               //class Alloc = std::allocator<pair<const key, T> >
             >
    class map
    {
        
    };
}
